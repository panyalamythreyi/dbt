selectors:
  - name: dim_except_listings_w_hosts
    description: Selects all dim models, excluding dim_listings_w_hosts.
    definition:
      union:
        - method: path
          value: models/dim
        - exclude:
            - method: fqn   #full qualified name
              value: dim_listings_with_hosts
